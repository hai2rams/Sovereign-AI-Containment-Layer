# Tests

| Path | Scope |
|------|-------|
| `packages/core/src/**/*.test.ts` | Unit tests for core anchor adapters |
| `packages/t3-adapter/src/**/*.test.ts` | T3 adapter M0 behavior |
| `integration/` | Cross-package tests (M0 placeholder) |

Run from repository root:

```bash
npm install
npm run test
```

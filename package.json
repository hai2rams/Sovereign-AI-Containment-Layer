{
  "name": "sovereign-ai-containment-layer",
  "version": "0.2.0",
  "private": true,
  "description": "Clean rebuild of the Sovereign AI Containment Layer with architecture-first boundaries.",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "test": "npm run test --workspaces --if-present"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "UNLICENSED"
}

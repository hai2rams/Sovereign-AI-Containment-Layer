{
  "name": "sovereign-ai-containment",
  "version": "0.1.0",
  "private": true,
  "description": "Sovereign AI Containment Layer — architecture-first containment platform with T3 trust anchoring.",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run build -w @sovereign/t3-adapter && npm run dev -w @sovereign/api",
    "test": "npm run test --workspaces --if-present",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "build": "npm run build --workspaces --if-present"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "UNLICENSED"
}

"""Semantic policy panel — M0 placeholder."""

import streamlit as st


def render_semantic_rules_panel() -> None:
    st.subheader("Semantic rules")
    st.caption("M0 shell — policy engine not wired.")

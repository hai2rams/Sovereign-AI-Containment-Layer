"""Blast radius visualization — M0 placeholder."""

import streamlit as st


def render_blast_radius() -> None:
    st.subheader("Blast radius")
    st.caption("M0 shell — containment scope map pending.")

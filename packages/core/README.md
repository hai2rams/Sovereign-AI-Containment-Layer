# @sovereign/core

Containment domain types and the abstract `AnchorAdapter` boundary.

This package must **not** depend on `@terminal3/t3n-sdk` or T3 contract code. Inject a concrete adapter (e.g. `@sovereign/t3-adapter`) at the application edge.
